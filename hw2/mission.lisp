(defvar *moves-mission* '(1C-CROSSES 2C-CROSS 1C1M-CROSS 1M-CROSSES 2M-CROSS))
(defvar *start-mission* '(3 3 W))
(defvar *goal-mission* '(0 0 E))

(defun 1C-CROSSES (state)
    (cond
        ((null node) nil)
        ((equal (nth 2 state) w)
            (setq moved (list ()))
            (cond
                (())
            )
        )
        ((equal (nth 2 state) e)
            (cond

            )
        )
    )
)
